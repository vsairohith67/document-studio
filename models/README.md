# Model registry

`models.yaml` is the canonical allow-list. A model is not downloaded, enabled or distributed merely because it appears here. Evaluation must produce a reproducible benchmark, license record, immutable revision and checksum.
