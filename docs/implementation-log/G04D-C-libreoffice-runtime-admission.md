# G04D-C non-mutating LibreOffice runtime admission proof

## Scope

This branch contains proof infrastructure only. It does not implement `office.to-pdf`, G04D1, G04D2, G04F2 or G05. It adds no production dependency, operation, contract, UI, migration, adapter or runtime bundle.

Starting accepted main: `a664dea4755122deefceac9d6ef699a920acc398`.

Proof branch: `feat/g04d-c-runtime-admission`.

## Validation rubric

The security/provenance review uses these five fail-closed criteria:

- [ ] The independently acquired MSI matches every frozen byte, signature, timestamp and Windows Installer identity value.
- [ ] The actual MSI feature/component graph proves the candidate ownership model, and dynamic pre/post evidence shows no protected host mutation.
- [ ] The realistic synthetic conversion interface works only with an isolated profile and inside the separate zero-capability AppContainer/owned Job Object boundary.
- [ ] qpdf and PDF.js independently reopen the exact staged PDF while source, profile, process and network controls remain intact.
- [ ] Exact owned cleanup succeeds; minimal MSI uninstall restores pre-state; no unrelated process or path is altered.

Unchecked boxes remain proof gaps until the exact branch-head manual workflow finishes. Local static checks cannot convert them to runtime evidence.

## Implemented proof surfaces

- `.github/workflows/g04d-c-libreoffice-runtime-proof.yml` has only `workflow_dispatch` and uses separate `windows-2025` jobs for administrative image and minimal MSI.
- `G04DC.Common.psm1` pins acquisition identity; manually follows no more than eight policy-validated TDF MirrorBrain HTTPS redirects; rejects local, literal, private and other special-use targets; binds each TLS connection to one prevalidated public IP while retaining the hostname for SNI/certificate validation; records and verifies the connected endpoint; streams through a create-new non-shared exact-size handle; cleans only its exact failed download; exports/hashes the live MSI tables and both install/admin sequences; builds the feature/component/effect map; seals authoritative Windows Installer registration/cache state plus bounded host catalogs; and supplies typed fail-closed assertions. A mirror hostname is never a trust anchor: the exact byte, signature, timestamp and MSI identities remain mandatory.
- `Invoke-G04DCAdminImageProof.ps1` runs `msiexec /a`, verifies full-catalog non-mutation, manifests the extracted tree, runs version and conversion only inside the zero-capability AppContainer/Job boundary and cleans only its GUID marker-owned runner root.
- `Invoke-G04DCMinimalMsiProof.ps1` derives `INSTALLLEVEL=0`, ordered exact `ADDLOCAL`/`REMOVE`, and the public-property vector from the package. Native Windows Installer condition evaluation determines expected state for every selected conditioned component. Before installation, every mutating MSI-table row must map to a disabled component and every install custom action must be statically bounded; otherwise the mode seals `REJECTED` without invoking MSI. If that model closes, the proof requires exit 0 plus exact ARP and Windows Installer ProductState/cache/Products/Features/UpgradeCode/Components registration, exact installed feature/component/File-table state, protected non-mutation before and after smoke, an exit-0 exact uninstall without reboot, an absent install root before owned cleanup, and complete pre-state restoration.
- `G04DC.Sandbox.cs` is a proof-only launcher. It creates the separate zero-capability profile, launches suspended, assigns the Job Object before resume, records complete Job accounting plus resolved process/module identities and sampled TCP/UDP sockets, preserves structured failure evidence and deletes only that profile. The wrapper requires no loopback exemption and validates every dynamically loaded module against exact runtime/Windows roots and signer chains. It records ACL grants, bounded owned writable-root inventories, runtime/fixture immutability and package-storage/registry cleanup, but it truthfully rejects candidate admission because those observations do not empirically prove effective file access or outside-root write denial.
- `Invoke-G04DCLocalMsiReadOnlyValidation.ps1` can reproduce table/condition/effect analysis against the already accepted local MSI without installing or launching LibreOffice.
- `New-G04DCSyntheticOdt.ps1` creates the tiny licence-clean fixture. `verify-pdfjs.mjs` confirms the generated PDF opens and contains only the expected canary assertion.
- `Test-G04DCBoundaries.ps1` covers the requested static failure classes without installing or launching LibreOffice locally.

## Current evidence state

The local proof-only validation completed without launching or installing LibreOffice:

- Windows PowerShell 5.1 parses all retained proof scripts/modules, and its compiler accepts both proof-only C# helpers: the AppContainer/Job launcher and native MSI condition evaluator.
- The fail-closed harness defines 89 named cases. It covers the exact canonical first request, accepted cross-origin and multi-hop HTTPS MirrorBrain redirects, a pinned connected endpoint and deterministic DNS-rebinding rejection, the eight/ninth-hop boundary, loops, missing `Location`, downgrade/port/non-empty and empty-userinfo/fragment rejection, localhost and special-use IPv4/IPv6 targets, raw IP literals, final filename, truncation/oversize/chunked framing, complete redirect evidence, failed-download ownership, every frozen MSI identity field, and all previously retained conditioned-component, MSI-effect, Installer-state, process, module, network, output, cleanup, and file-access failures.
- Read-only inspection of the exact accepted local MSI derived a 23-feature, 581-selected-component Writer/Calc/Impress closure with no feature/component ownership ambiguity and no selected Font-table component. The marker-owned evidence is `C:\Dev\document-studio-worktrees\_resources\g04d-c-runtime-admission\local-msi-readonly-validation-9`; its artifact-manifest SHA-256 is `44704b9eceb165e291d83754b3166b2d0bbaad5c4ec6d0eb1eed044ccb5ec344`.
- Exact Directory/Component/File analysis found 29 selected VC runtime components outside `INSTALLLOCATION`; native condition evaluation under derived `VC_REDIST=0` must prove each absent, while the exact 29 system targets remain unchanged. All other selected conditions are evaluated under the same candidate property vector.
- Every relevant standard MSI mutation table and both administrative sequences are exported. The exact package produced 1,937 mapped mutation rows, zero ambiguous rows, 25 enabled protected rows (20 `Registry`, five `Shortcut`), 34 unbounded install/UI custom-action sequence entries and zero unbounded administrative custom actions. The minimal MSI model therefore fails closed before installation; these static blockers are not waived in favor of dynamic measurement. Dynamic state sealing remains required for any mode whose static model closes.
- Current AppContainer evidence does not include empirical effective file-access telemetry. The smoke therefore fails closed with `FILE_ACCESS_BOUNDARY_INVALID`; inventories and ACLs are never reported as a passing substitute.
- `scripts/validate_repo.py`, `scripts/check_links.py`, `git diff --check`, Node syntax validation and YAML structure validation passed.
- Every pinned GitHub action commit resolved through the upstream GitHub repository API.

Runtime candidate classification, exact workflow head/run URLs and artifact hashes must come only from the manual exact-head GitHub Actions run.

Preliminary local source-boundary observation on 2026-08-28: the exact owner-specified `download.documentfoundation.org` URL returned an HTTP 302 to a MirrorBrain-selected mirror origin. G04D-C2 now permits that acquisition shape only under the bounded redirect and frozen cryptographic/MSI-identity policy above. This local observation is not exact-head CI evidence and is not a runtime result; the manual exact-main run must seal the then-current chain and package.

The workflow result is an architecture-gate input, not G04D-B2 acceptance or production support.

## G04D-C3 registry collector repair

The first exact-main administrative-image run (`33175958332`) stopped before constructing or invoking `msiexec /a`. Its pre-state collector used `Get-ItemPropertyValue -Name '(default)'` for `HKEY_CLASSES_ROOT\.odt`; the key existed on the runner but had no default value, so fail-closed error handling correctly stopped the proof while the collector incorrectly lacked a representable state for that valid registry shape. This was a proof-collector defect and produced no LibreOffice extraction, containment, network, conversion, or runtime-support conclusion.

G04D-C3 replaces every targeted `Get-ItemPropertyValue` collector site with one bounded read-only registry-value state helper. Default-value evidence now has three explicit shapes: missing key (`keyExists=false`, `defaultValuePresent=false`), existing key without a default (`keyExists=true`, `defaultValuePresent=false`), and a present default (`keyExists=true`, `defaultValuePresent=true`, plus `defaultValueType` and `defaultValue`). Absent states omit type and value rather than serializing an ambiguous null; a present empty `REG_SZ` remains an empty string with `defaultValuePresent=true`. Named monitored values use the parallel `keyExists` / `valuePresent` / `valueType` / `value` schema.

The helper opens only bounded `Registry::HKEY_CLASSES_ROOT`, `HKEY_CURRENT_USER`, `HKEY_LOCAL_MACHINE`, or `HKEY_USERS` paths for read access, requests unexpanded values, preserves legitimate registry kinds, bounds serialized value bytes, rechecks a value that disappears during classification, and fails closed on access denied or any provider/API failure that prevents a trustworthy state. The PowerShell 5.1 matrix uses only a GUID-owned HKCU test subtree for dynamic registry cases; `.odt`, `.ods`, and `.odp` are never changed under the real association roots. The existing MSI provenance, minimal-MSI rejection, MirrorBrain/HTTPS/SSRF/TLS, AppContainer, Job Object, network, effective file-access, output-verification, and cleanup gates are unchanged.

## G04D-C4 heterogeneous scheduled-task collector repair

Exact-main manual proof run `33324659271` stopped before constructing or invoking `msiexec /a`. The machine-state collector projected `Execute`, `Arguments`, and `WorkingDirectory` from every object in `ScheduledTask.Actions`; a non-Exec action did not expose `Execute`, so PowerShell StrictMode raised `PropertyNotFoundStrict`. This is a proof-collector failure and provides no LibreOffice extraction, AppContainer, Job Object, network, file-access, conversion, or runtime-support result.

G04D-C4 centralizes property metadata inspection and typed action conversion. Every action now records its zero-based index, exact CIM class, normalized `exec`, `comHandler`, `email`, `showMessage`, or `other` kind, and a canonically ordered bounded property object. Missing properties are omitted, present null and empty-string values remain distinct, scalar and array shapes are retained, strings and arrays have explicit ceilings, and complex recursive values fail closed. COM `Data`, email payload/address fields, and show-message text retain only their typed shape and deterministic SHA-256 evidence so unrelated task content does not enter uploaded machine-state artifacts; identifiers and the previously authorized Exec fields retain their bounded typed values. Unknown but identifiable action classes retain only present bounded `Id`/`Name` identifiers. Action order remains execution order and tasks remain ordered by `TaskPath` plus `TaskName`.

`Export-ScheduledTask` remains mandatory for every task. Its single bounded parseable XML definition is hashed as `definitionSha256` and is not added to ordinary logs or structured artifact contents; that hash remains authoritative for definition changes outside the supplemental action summary. Task enumeration, action identity, property retrieval, XML export, XML parsing, bounds, or stable ordering failures now stop collection explicitly. The collector never calls a scheduled-task mutation command.

The focused Windows PowerShell 5.1 suite now defines 134 cases. Its 30 G04D-C4 additions cover normal and optional Exec fields, COM, email and show-message shapes, mixed and unknown actions, order, absent/null/empty distinctions, scalar/array preservation, hash-only treatment of sensitive non-Exec payloads, XML-hash coverage, export/getter failures, string/array ceilings, recursive and non-finite value rejection, deterministic equality/change behavior, zero mutation, a GitHub-hosted-runner-shaped COM fixture, removal of the direct `.Execute` assumption, and task ordering. A scoped scan found no other G04D-C proof collector that mixes heterogeneous child CIM classes inside one unguarded property projection; the homogeneous service and per-filter firewall collectors were therefore left unchanged.

## G04D-C5 bounded machine-state capture and PRECHECK

G04D-C5 starts from accepted main `743dfc69d28e77b3a1b576ccd60387caaeabaff0` on `feat/g04d-c5-machine-state-performance`. Manual proof run `33359064703` was dispatched at that exact main and ended `PROOF_PROVENANCE_OR_INFRASTRUCTURE_FAILURE`: the administrative job reached its 90-minute job ceiling during its initial machine-state capture before `machine-pre.json` was written. The accepted administrative artifact contains no administrative-extraction command, so the run did not construct or invoke the administrative extraction and is not a LibreOffice runtime failure. Its independently manifest-verified MSI analysis established scale only: 1,680 feature rows, 6,092 component-to-feature rows, 6,092 component-install ownership rows, 19,494 file-component rows, 1,793 protected registry rows, zero unbounded administrative custom actions and 34 unbounded install custom actions.

The frozen mutation boundary remains unchanged. Every capture still seals Office/default associations; machine/user font registry plus exact MSI font files; exact external VC/runtime targets; the service catalog and full Services registry digest; App Paths; the class-key catalog, full HKCR digest and LibreOffice ProgIDs; startup registry/folders; all scheduled-task definitions and bounded action evidence; Start Menu/Desktop shortcut trees; machine/user environment; firewall rules and filters; every MSI-derived protected registry target; LibreOffice processes; installed-product state; the full Windows Installer cache; pending reboot state; exact product/upgrade/component registration; and the ordinary LibreOffice profile boundary. G04D-C5 does not remove, sample or narrow any of those surfaces and does not change `Compare-G04DCMachineState`, `Assert-G04DCMsiRegistrationAbsent` or `Assert-G04DCMsiRegistrationInstalled` truth conditions.

Machine-state capture now has an optional external append-only NDJSON progress sink and a separate performance summary. The progress file is opened before the first phase, every record is immediately flushed to stable storage, and records contain only schema version, capture UUID/label, monotonic sequence, event, generic phase identifier, monotonic elapsed milliseconds, bounded item count and status. It never contains captured registry values, file contents, task XML, user names, credentials or unrelated paths. The performance summary records total duration, every phase duration/count, the 8-minute target, 12-minute hard ceiling, 4-minute per-phase ceiling and pass/fail. Canonical state finalization and durable `machine-*.json` serialization are explicit bounded phases; proof commands are gated on the sealed passing performance artifact. Telemetry is outside the canonical machine-state object and hashes.

The independently timed phases are associations, font registry, protected font files, external runtime targets, services, Services registry digest, App Paths, class-key catalog, HKCR digest, startup, scheduled tasks, shortcuts, environment, firewall, protected registry targets, processes, installed products, Installer cache, pending reboot, MSI registration, canonical state finalization and durable state serialization. The collector checks the monotonic deadline inside component, protected-registry and directory/hash loops; large provider/CIM/firewall inventories use bounded out-of-process helpers inside kill-on-close Job Objects; durable JSON serialization and file SHA-256 reads use cooperative bounded streaming; and native `reg.exe` is assigned to the same owned process boundary with verified terminal exit. A breach flushes evidence and fails closed as `[MACHINE_STATE_CAPTURE_BUDGET_EXCEEDED]` with only phase, elapsed and count data; proof scripts cannot continue to Windows Installer afterward. The full workflow remains at 90 minutes.

The primary modeled hot path was exact component registration: 6,092 component codes previously caused more than 12,000 independent Registry-provider path classifications per capture. G04D-C5 keeps the same output fields but uses `Microsoft.Win32.RegistryKey` read-only access in `Registry64`, matching the 64-bit Windows PowerShell workflow/provider view. It opens the system and user component-registration roots once, opens each packed component subkey relative to those handles, preserves missing-key, missing-value, empty-value, typed-value and disappearance-race semantics, and disposes every component/root handle. No key is created, no write permission is requested, `Win32_Product` is not used and Windows Installer self-repair is not triggered. The 6,092-code synthetic scale test proves two bounded base-root opens, 12,184 exact relative subkey attempts, 6,092 deterministic output rows and no per-component provider helper call.

The manual workflow remains `workflow_dispatch` only and now accepts `proofMode=precheck|full` with `full` as the default. PRECHECK has a 20-minute job ceiling, repeats exact MSI acquisition/provenance and database derivation, performs one 12-minute-bounded initial capture, verifies runner isolation and registration absence, and uploads manifest-sealed `machine-pre.json`, progress and performance evidence. It contains no administrative extraction, runtime launch, AppContainer execution or candidate classification. The existing two-runner full proof is gated unchanged behind `proofMode=full`; all full captures now retain durable per-capture timing evidence.

The focused Windows PowerShell 5.1 suite now defines 186 cases. The C5 additions cover progress creation/flushing/order/sequence/monotonic time/privacy/failure, successful durable serialization, phase/overall/final-success/serialization/hash budgets, durable failure artifacts, bounded canonical-hash equivalence and terminal expiry, streaming-hash disposal, sealed out-of-process helper output and terminal timeout cleanup, exact component semantic equivalence including whitespace-malformed input, system/user/both scopes, access/read failures, disappearing values, disposal, Registry64/provider equivalence, the 6,092-component algorithmic scale, PRECHECK isolation, frozen full path/performance gates/timeouts and the proof-only diff boundary.

## G04D-C6 Windows PowerShell 5.1 source compatibility

Exact-main PRECHECK run `33382689804` failed before `Invoke-G04DCMachineStatePrecheck.ps1` began execution. The script was UTF-8 without a BOM and contained an em dash in two executable source strings. Windows PowerShell 5.1 decoded those bytes through the runner's legacy ANSI code page, produced malformed source and stopped in its parser. The run created no PRECHECK evidence directory, acquired no MSI, produced no `machine-pre.json`, and neither constructed nor invoked `msiexec /a`. This is source/bootstrap infrastructure evidence, not a LibreOffice or C5 machine-state result.

G04D-C6 starts from accepted main `02d29ee7f6fcab5f721d683c91c31a87c588d856` on `feat/g04d-c6-powershell51-encoding` in the dedicated `g04d-c6-powershell51-encoding` worktree.

G04D-C6 freezes one byte-level policy: every `.ps1` and `.psm1` beneath `scripts/g04d-c/` must contain only tab, CR, LF, and bytes `0x20` through `0x7E`. UTF-8, UTF-16 and UTF-32 BOMs are rejected. Documentation and explicitly non-executable fixtures remain outside that source boundary. Unicode data needed by executable tests is constructed from ASCII code-point literals at runtime rather than embedded in script bytes.

`scripts/g04d_c_powershell_source_policy.py` performs deterministic raw-byte discovery and reports only a repository-relative path plus byte offset for each violation. Normal repository validation imports the same helper. `Test-G04DCPowerShell51Source.ps1` requires the actual Windows PowerShell 5.1 Desktop engine, consumes the raw-byte report, then calls the parser API without executing any inspected script. The focused verifier, normal Windows CI, and proof workflow run this gate and log the engine version, source count, ASCII status and parser status.

PRECHECK now begins with a YAML-owned fail-safe bootstrap only: it creates the exact evidence directory and ASCII marker before any repository script runs. The source gate seals the checked-out SHA, Windows PowerShell version, source count and gate outcomes in `bootstrap-source-validation.json`. Only a passing bootstrap may enter PRECHECK; the script then marks `precheckScriptStarted=true` before acquisition. A parser/bootstrap failure therefore uploads truthful marker/bootstrap evidence through the existing always-run path without fabricating machine-state, MSI acquisition, extraction or classification evidence.

The focused suite now defines 206 cases. Its C6 additions cover ASCII `.ps1`/`.psm1` acceptance; UTF-8-no-BOM em dash and smart-quote rejection; UTF-8, UTF-16 LE/BE and UTF-32 LE/BE BOM rejection; non-ASCII comment/string rejection; invalid-ASCII parser rejection; actual Windows PowerShell 5.1 acceptance; Unicode documentation and explicit non-executable fixture isolation; exact relative path/byte-offset reporting; deterministic discovery; current PRECHECK compatibility; workflow-invoked source coverage; and zero incomplete, malformed or unknown parser tokens.

Local C6 validation on 2026-08-31 used Windows PowerShell 5.1.26100.9168 and passed the explicit source gate over 12 files, all 206 focused cases, the focused verifier, locked repository validation, link checking, `git diff --check`, npm typecheck, 185 Vitest tests, the production frontend build, Cargo formatting/check/clippy/tests and the Tauri no-bundle release build. The Tauri build retained the existing non-blocking `.app` identifier advisory. No local LibreOffice process, profile, MSI operation or proof workflow was invoked.

## G04D-C7 class-registry digest performance repair

G04D-C7 starts from accepted main `edf79caaa454d4760c5a92d93b889d77f748dc26` on `feat/g04d-c7-class-registry-digest` in the dedicated `g04d-c7-class-registry-digest` worktree. Exact-main PRECHECK run `33393424855` produced manifest-valid machine-state telemetry and stopped in `class-registry-digest` before `machine-pre.json`, `msiexec`, administrative extraction or runtime launch. Its phase began at 51,521 ms, the first post-query progress appeared at 280,406 ms, and the phase stopped at 240,019 ms after 64,236 canonical rows. This local artifact analysis attributes about 228.9 seconds to the native query/read interval; it is performance evidence only and makes no runtime-support conclusion.

The first authorized repair retains the full merged 64-bit `HKEY_CLASSES_ROOT` surface and the existing canonical digest meaning. `reg.exe query HKEY_CLASSES_ROOT /s` remains the read-only source, is launched directly without a shell, and remains assigned to the kill-on-close Job Object. A small local C# helper consumes stdout incrementally under an explicitly selected strict host-console output encoding and separately bounded stderr capture. Raw bytes, row count, row length, canonical row bytes, aggregate canonical bytes, stderr bytes and stream-buffer size all have fixed ceilings. The helper preserves the prior CRLF/LF split, single terminal-empty-row removal, `TrimEnd`, PowerShell 5.1 JSON-string representation, current-culture case-insensitive sort and SHA-256-with-LF model. It creates no raw registry-output file and contains no registry or filesystem mutation path.

`class-registry-digest` remains one externally timed phase with the unchanged 240,000 ms hard phase ceiling. Its telemetry now includes `native-query-startup`, `native-query-read`, `row-normalization`, `canonical-hash` and `helper-cleanup` substages with elapsed time and counts only; no registry names, values or paths enter progress telemetry. Every wait, decode, normalization and hash boundary remains deadline-aware. Timeout and fault paths terminate and verify the owned helper before surfacing failure, and PRECHECK additionally requires the complete class-registry phase to finish within the 180,000 ms engineering target.

Exact feature-head PRECHECK run `33410565753` at `1225f349091ca484398e491b3f0d04a6ed2c9705` independently sealed all 28 declared artifact files and passed the C7 target: the complete class-registry phase took 130,906 ms for 583,187 rows, comprising 3 ms startup, 128,370 ms native read, 332 ms normalization, 2,190 ms canonical hash and 1 ms helper cleanup. The run later stopped in the unchanged `shortcut-catalog` after 254 entries, before `machine-pre.json`, isolation acceptance or MSI-registration acceptance. No Windows Installer or runtime command ran. The first focused repair therefore leaves HKCR unchanged and adds four-attempt, 250 ms bounded retries only for transient directory-entry I/O/not-found races; each successful file hash is bound to matching before/after size and last-write metadata, while access failures, reparse changes, persistent instability and budget expiry remain terminal and path-free.

The next exact feature-head PRECHECK run `33415616489` at `29849d3b55cba6fa3bc6e738dc7ff44a66d4e632` independently verified all 28 outer artifact entries plus all 15 present MSI-table entries with zero path, size or SHA-256 errors. It again stopped at `shortcut-catalog` after 254 entries. More importantly for C7, native-query dominance recurred: the complete class-registry phase took 229,693 ms for 584,187 rows, comprising 4 ms startup, 226,597 ms native read, 460 ms normalization, 2,617 ms canonical hash and 1 ms cleanup. That exceeded the 180,000 ms C7 target while remaining beneath the unchanged 240,000 ms hard phase ceiling. Total capture at failure was 291,044 ms. The run produced no `machine-pre.json`, invoked no Windows Installer command and launched no runtime.

That exact-run evidence activates the prompt-authorized second and final focused fallback. Production HKCR capture now opens `RegistryHive.ClassesRoot` through `RegistryView.Registry64`, which is the operating system's merged per-user/per-machine classes view, and uses only read-only `RegistryKey` enumeration plus `RegQueryValueExW`. Digest schema 2 is deterministic binary framing over every key path and every value name, native type code and exact stored bytes. It therefore preserves default absent/present-empty distinction, non-expanded expandable strings, binary and multi-string bytes, DWORD/QWORD data, unsupported high-level value kinds, and type changes without putting raw registry material in telemetry or artifacts. Keys and values are ordinally sorted; key count, value count, depth, per-value bytes and aggregate canonical bytes are independently bounded. Two complete traversals must return identical counts, byte totals and SHA-256 or capture fails closed. Access denial, a listed-but-unopenable key, a disappearing key/value, unstable membership and deadline expiry remain terminal. Every handle and in-memory canonical buffer is disposed, and no registry write, remote registry, WMI or shell path exists.

The five established substages remain within the single unchanged phase timer. Privacy-safe progress is flushed every 4,096 rows; direct traversal reports separate measured read, normalization and hash costs without changing the phase deadline. The shortcut collector still hashes every enumerated file and keeps before/after size and last-write binding; its fail-closed wait is now 20 attempts at 500 ms so a longer runner-owned sharing interval can settle. It never skips, substitutes metadata-only evidence or accepts a persistently unreadable entry.

The focused Windows PowerShell 5.1 suite now defines 280 cases. C7 coverage includes exact legacy streaming normalization/hash equivalence; zero, terminal-empty, CRLF-boundary, trailing-space, 64,236-row and maximum-bound cases; split and malformed multibyte UTF-8; stdout/stderr and aggregate ceilings; normalization/hash deadlines; non-zero native exit; Job Object timeout termination; substage order/timing/privacy; and GUID-owned HKCU fixtures exposed through the merged 64-bit HKCR view for key create/delete/nesting/rename, absent/empty/default values, string/expand-string/multi-string/binary/DWORD/QWORD values, type/data/deletion changes, Unicode, long values, repeatability and zero mutation. Direct-fallback cases additionally prove schema/counts, schema/key/value-count comparison, real access denial, deterministic disappearing-key/value races, key/value/depth/value-byte/canonical-byte ceilings, timeout, double-traversal stability and handle cleanup. The local fixture changes only its exact test-owned `HKCU\Software\Classes\DocumentStudioG04DCTest_*` subtree and is removed afterward; it does not profile or mutate the user's full HKCR and does not install or launch LibreOffice.

Local C7 validation on 2026-08-31 used Windows PowerShell 5.1.26100.9168 and passed the explicit source gate over 12 PowerShell files, all 280 focused cases, the focused verifier, locked repository validation, link checking, `git diff --check`, npm typecheck, 185 Vitest tests, the production frontend build, Cargo formatting/clippy/all workspace tests and the Tauri no-bundle release build. The Tauri build retained the existing non-blocking `.app` identifier advisory. No local LibreOffice process, ordinary profile, MSI operation or proof workflow was invoked.

## G04D-C8 P1 helper-cleanup and canonical-encoding remediation

G04D-C8 continues on PR #25 from exact feature head `4f59211e24be0eec49e37b83d51f19c0e4869aa3` and accepted main `edf79caaa454d4760c5a92d93b889d77f748dc26`. It addresses the two live P1 review findings without changing the direct HKCR traversal, retry policy, phase ceilings, proof trigger, acquisition identity or runtime classification gates.

The bounded native registry-query path now preserves operation, native-cleanup and telemetry failures separately. Its mandatory cleanup path runs before any cleanup evidence write: an unproven running helper is terminated and verified through its owned kill-on-close Job Object; `Process.Dispose` is attempted regardless of termination outcome; Job Object disposal is attempted regardless of process-disposal outcome; and both redirected readers, their base pipe streams and completed reader tasks are explicitly disposed in independently protected cleanup steps. The path does not treat `Process.Dispose` as ownership transfer for redirected pipes. Each operation is isolated by nested `try`/`catch`/`finally` blocks. Native-cleanup failure has first precedence, the original capture failure has second precedence, and post-cleanup telemetry persistence failure has third precedence. The terminal message retains only bounded stage and boolean secondary-failure evidence. Synthetic-only hooks are rejected outside the existing test-native path. Active Job Object handles and retained pipe readability are checked so failure tests can prove exact cleanup without process-name lookup, finalization or global termination.

Native decoding and canonical hashing are now separate concepts. The explicitly selected strict console encoding is used only for process stdout/stderr decoding. Canonical row byte ceilings, aggregate canonical byte counts and SHA-256 input use a dedicated strict `UTF8Encoding(false, true)`. The accepted `TrimEnd`, JSON-string quoting/escaping, current-culture case-insensitive ordering, row count and LF separator model are unchanged, and canonical hash bytes never include a BOM.

The focused Windows PowerShell 5.1 suite now defines 315 cases. Its 19 C8 cleanup additions cover normal process/Job disposal; explicit stdout/stderr reader, pipe-stream and completed-task disposal; timeout, cancellation and native-reader failure termination; progress, substage-start, substage-end, cleanup, durable-flush and simulated disk-full telemetry failures; termination/process-disposal fault ordering; sanitized failure precedence; no orphan helper, readable pipe or Job handle; unrelated-process survival; recovery on the next invocation; and absence of global process termination. Its 16 encoding additions cover UTF-8, Windows-1252, OEM and UTF-16 native decoding; ASCII and Unicode digest equality; Devanagari/Telugu UTF-8 byte counts; row and aggregate ceilings; no BOM; LF/empty-row/stdout/stderr semantics; malformed bytes; exact native-decoder selection; host-console independence; and the unchanged accepted hash fixture.

Local C8 validation on 2026-09-01 used Windows PowerShell 5.1.26100.9168 and passed the 12-file ASCII/parser source gate, the 315-case focused suite three successive times, the focused verifier, locked repository validation, link checking and `git diff --check`. The unchanged application also passed `npm ci --ignore-scripts`, typecheck, all 185 Vitest tests, the production frontend build, Cargo formatting/check/clippy/all workspace tests, and the Tauri no-bundle release build. The Tauri build retained the existing non-blocking `.app` identifier advisory. No local LibreOffice process, ordinary profile, MSI operation or proof workflow was invoked.

## G04D-C9 typed HKCR failure provenance

G04D-C9 starts from accepted main `0635136bd3e4e358be7f61782dae7c7f0a08d0db` on `feat/g04d-c9-hkcr-stability` in the dedicated `g04d-c9-hkcr-stability` worktree. Exact-main PRECHECK run `33661246601` failed during `class-registry-digest` after 13,932 ms, with 13,907 ms in the direct native-query-read substage. Manifest-verified progress reached 208,663 pass-local rows and 6,824,910 raw bytes without an observed row-count reset; the evidence therefore does not clearly show entry into a second traversal pass. Helper cleanup completed, the unrelated-process sentinel survived, and the total capture elapsed time was 63,115 ms. `machine-pre.json` was absent, and the PRECHECK workflow did not dispatch the full proof, construct or invoke `msiexec /a`, or launch LibreOffice. The sealed result retained only `MACHINE_STATE_CAPTURE_FAILED`; no lower-level traversal reason is inferred from that artifact.

The diagnostic head introduces a sealed `RegistryTraversalException` and a closed `RegistryTraversalFailureCode` enum. The exception carries only the allow-listed code, an optional native status integer, attempt/pass indexes, pass-local key/value/row counts, raw-byte count and elapsed milliseconds. Direct traversal creates this typed failure at the source for access denial, disappearing keys or values, failed value reads, instability, every structural ceiling, deadline expiry and unknown internal failures. Messages remain generic and never carry a registry path, key name, value name, value data, username, stack trace or unrestricted native exception text.

One bounded PowerShell extractor walks at most eight exception objects, including method-invocation and reflection wrappers, and accepts only the exact typed C# object. Unknown, over-depth or malformed failures become `REGISTRY_TRAVERSAL_INTERNAL_FAILURE`. The compatibility result remains `MACHINE_STATE_CAPTURE_FAILED` for non-timeout traversal failures while `detailReasonCode` and bounded fields are sealed separately in PRECHECK, capture-failure telemetry and the performance artifact. Failure performance and PRECHECK result JSON are flushed durably before marker-owned cleanup begins. Direct-HKCR progress now distinguishes attempt index, pass index, pass-local row count and traversal elapsed time; it records attempt start, pass start/progress/end and attempt success without registry content. No retry or stabilization behavior is included in the diagnostic head.

The focused Windows PowerShell 5.1 suite now defines 327 cases. The 12 C9 diagnostic additions cover every closed failure code through PRECHECK evidence, wrapped extraction, exact inner-exception depth, unknown failure fallback, raw-message and registry-content omission, bounded attempt/pass/count/native-status evidence, generic compatibility, durable failure flushing before cleanup, a bounded 208,663-row telemetry scale, actual C# `MethodInvocationException` retention and deterministic content-free two-pass telemetry. Existing limits, two-complete-pass digest acceptance, full HKCR coverage, workflow timeouts, acquisition identity and proof-only boundaries remain unchanged. The first feature-head PRECHECK is required before any transient stabilization decision.

Diagnostic PRECHECK run `33669325149` executed against exact feature head `bab91318bf79b70559742ed02bbe5920a941f134`. Its sole artifact matched GitHub's declared 2,493,348-byte envelope and `fb20e3bb599e19ff95f47c523b76af619e8a8b01d75d0b8892c1fedec5096c00` SHA-256; its outer 28-file manifest and nested 25-table manifest verified exactly, with no unsafe path, undeclared outer file or reparse point. The sealed result retained `REGISTRY_TRAVERSAL_UNSTABLE` at attempt 1, pass 1 after 207,906 rows (98,632 keys and 109,274 values), 6,774,790 raw bytes and 12,893 traversal milliseconds. Total capture time was 55,746 ms, the class-registry phase was 12,995 ms, helper cleanup succeeded, the unrelated-process sentinel survived and `machine-pre.json` was absent. The PRECHECK-only run skipped the administrative-image, minimal-MSI and decision jobs. Because instability is one of the owner's three explicitly authorized transient codes, this exact evidence opened the bounded stabilization gate.

The stabilization collector permits at most three whole-capture attempts under the original single stopwatch and unchanged 240,000 ms phase budget. Every attempt creates fresh Registry64 roots and requires two independently opened, complete traversals with exact key, value, raw-byte and canonical-byte counts plus identical SHA-256. A disappearing key, disappearing value or instability discards all returned and observed attempt state after buffer and handle unwinding; attempts 1 and 2 then publish a content-free retry record and wait exactly 250 ms or 500 ms only when the unchanged deadline can contain that delay. Access denial, failed value reads, timeouts, ceilings, internal failures and unknown codes are never retried. Three transient failures seal `REGISTRY_TRAVERSAL_STABILITY_EXHAUSTED` plus the last transient reason. The focused suite now defines 346 cases, adding all 19 stabilization cases for pass equality, authorized retry classification, exhaustion, state discard, fresh-handle disposal, deterministic delays, global deadline behavior, non-retried codes, content-free telemetry, test-owned registry mutation and a 208,663-row bounded scale.

## Explicit non-actions

- No ordinary full MSI install occurs on the laptop.
- No user document or ordinary LibreOffice profile is accessed.
- No manual font, service, association, UserChoice, registry or broad filesystem cleanup occurs.
- No production code, merge, tag, package, release, deployment or tracker update is included.
