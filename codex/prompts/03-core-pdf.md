# Codex Prompt 03 - Viewer and Core PDF

Continue the approved G03 goal on `feat/g03-viewer-core-pdf` and existing draft PR #6. Preserve all accepted G01/G02 commands and guarantees. Maintain the local Document Studio viewer with opaque retained-handle sessions, transactional active/candidate replacement, raw bounded range IPC, an exact locally pinned PDF.js asset manifest, measured viewport visibility, fail-closed canvas limits, safe text search and deterministic cleanup. Maintain the accessible organizer, including Alt+Arrow block reorder, and the five typed qpdf operations: extract, remove, reorder, rotate and split.

Use migration 4 only for canonical, hashed, 2–65,536-byte operation-plan envelopes. Use only bundled qpdf 12.3.2, direct ASCII workspace arguments, the accepted sandbox/cancellation/publication architecture and independent structure/count/rotation/hash verification. Pre-create all split output rows and represent partial publication as failed without deleting published user files.

Do not add compression, repair, OCR, conversion, editing, annotations, forms, signatures, redaction, printing, cloud, accounts, AI, another dependency/migration/capability, a custom protocol or COM drop. Keep PR #6 draft and unmerged. G03 remains in Review until a fresh independent exact-head re-review accepts it; G04 remains blocked.
